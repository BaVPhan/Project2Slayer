<!DOCTYPE html>
<html>
  <head>
    <title>Slayer</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Anton|Permanent+Marker" rel="stylesheet">
  </head>
  <nav class = "slaybar">
    <img src="<%= asset_path( 'hammer.png' ) %>" alt="hammer logo" style="width:50px;height:50px;"/>
    <h1><%= link_to "Slay.R", root_url %></h1>
    <img src="<%= asset_path( 'sword.png' ) %>" alt="sword logo" style="width:50px;height:50px;"/>
  </nav>
  <nav class="slaybar">
  <% if @current_user %>
    <%= link_to "Welcome, #{@current_user.username}!", user_path(@current_user) %>
    <%= link_to "Sign Out", session_path, method: :delete %>
  <% else %>
    <%= link_to "Sign Up", new_user_path %>
    <%= link_to "Sign In", new_session_path %>
  <% end %>
  </nav>
  <% flash.each do |type, message| %>
  <p class="<%= type %>"><%= message %></p>
  <% end %>
  <nav class="navbar">
    <%= link_to "Monsters", monsters_path %>
    <%= link_to "Heroes", heros_path %>
    <%= link_to "Existing Users", users_path %>
  </nav>
  <body>
    <%= yield %>
  </body>
  <footer>
    <p> Created by: Ba Phan</p>
  </footer>
</html>
